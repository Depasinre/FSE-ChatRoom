# FSE-ChatRoom
It's a ramp project for 18-652

## run on local
First, \\
`npm install` \\

To test in development enviroment with nodemon, run: \\
`npm run dev` \\

To run only once, run: \\
`npm run start` \\

